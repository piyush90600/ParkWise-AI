document.querySelectorAll('[data-action]').forEach(button => { button.addEventListener('click', () => { const row = button.closest('tr'); const badge = row.querySelector('.status-pending'); const action = button.dataset.action; badge.textContent = action === 'approve' ? 'Approved' : 'Rejected'; badge.className = `status-pending status-${action === 'approve' ? 'approved' : 'rejected'}`; row.querySelector('.verification-actions').innerHTML = '<span>Updated</span>'; }); });
